<nav class="navbar navbar-expand-lg bg-body-tertiary shadow">
  <div class="container-fluid">
    <a class="navbar-brand" href="/projects">
      <img src="/uploads/logo.jpg" alt="Hanu" width="40" height="40" class="rounded-circle" />
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/projects">
            Project</a>
        </li>
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/users">User</a>
          </li>
        <% } %>
      </ul>

     
      <div class="d-flex align-items-center ms-auto">
        <% if (user) { %>
          <span class="me-3">Hello, <%= user.name %>!</span>
          <a class="nav-link text-danger fw-bold" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link btn btn-outline-warning px-3 rounded-pill" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
